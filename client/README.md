# Color Palette Maker — React Client

React + Vite frontend for the Color Palette Maker. Part of the monorepo; run from the project root.

## Commands (from project root)

```bash
npm run dev          # Start backend + client (or npm run dev:client for client only)
npm run build        # Build for production
npm test             # Run Vitest
cd client && npm run test:watch   # Watch mode
cd client && npm run lint         # ESLint
```

See [docs/DEVELOPMENT.md](../docs/DEVELOPMENT.md) for full setup and project structure.
