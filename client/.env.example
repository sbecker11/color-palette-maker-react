# Client-side config (Vite injects VITE_* at build time).
# Copy to .env in this directory to override.

# Highlight region interiors (fill/stroke) on hover. Set to false to disable.
VITE_HIGHLIGHT_REGION_ON_ROLLOVER=true
