# Region detection requires Python with opencv-python and numpy.
# Override Python path if auto-detection fails:
# DETECT_REGIONS_PYTHON=/path/to/python

# Server port (optional; defaults to 3000)
# PORT=3000
