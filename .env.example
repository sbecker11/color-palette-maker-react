# Server port (default: 3000)
PORT=3000

# Python path for region detection (optional; defaults to venv/bin/python or python3)
# DETECT_REGIONS_PYTHON=/path/to/python

# Palette luminance thresholds (0-255). Pixels and clusters outside this range are excluded.
# Default: MIN=25 (excludes near-black), MAX=185 (excludes near-white)
# To include black/white in palettes, use MIN=0 and MAX=255
MIN_LUMINANCE_THRESHOLD=25
MAX_LUMINANCE_THRESHOLD=185
